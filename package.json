{
  "name": "desafio-backend-senior-ii",
  "version": "1.0.0",
  "description": "Desafio Técnico Backend Sênior II - Matrículas e Bolsas",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node src/seed.js",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "test": "jest --runInBand --detectOpenHandles",
    "test:watch": "jest --watch",
    "test:coverage": "jest --runInBand --coverage"
  },
  "keywords": [
    "nodejs",
    "express",
    "mongodb",
    "mongoose",
    "backend",
    "senior"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "20.x || 22.x || 24.x"
  },
  "config": {
    "mongodbMemoryServer": {
      "version": "7.0.14"
    }
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.4"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "mongodb-memory-server": "^9.2.0",
    "nodemon": "^3.1.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "testTimeout": 120000,
    "coveragePathIgnorePatterns": [
      "/node_modules/",
      "/tests/"
    ]
  }
}
